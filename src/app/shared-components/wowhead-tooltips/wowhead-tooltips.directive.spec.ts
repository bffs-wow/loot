import { WowheadTooltipDirective } from './wowhead-tooltip.directive';

describe('WowheadTooltipsDirective', () => {
  it('should create an instance', () => {
    const directive = new WowheadTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
